#include "stdafx.h"
/*
#include "stdafx.h"

#include <stdio.h>
#include <cstring>

int countZeros(int k)
{
    int count = 0;

    while (k)
    {
        int remain = k % 10;
        k /= 10;
        if (!remain)
            count++;
    }

    return count;
}

int getHowManyZeros(int m, int n) {
    int total = 0;
    for (int i = m; i <= n; i++) {
        total += countZeros(i);
    }

    return total;
}

int main(int argc, char* argv[])
{

    int n, m;
    while (!scanf("%d%d", &m, &n))
    {
        printf("%d\n", getHowManyZeros(m, n));
    }

    return 0;
}

*/